# Socialink Live Streaming Service Configuration

# Service
PORT=8098
GIN_MODE=release

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/socialink_streaming?sslmode=disable

# Redis (for real-time features)
REDIS_URL=redis://localhost:6379/0

# Kafka (for events)
KAFKA_BROKERS=localhost:9092

# Media Service (gRPC)
MEDIA_SERVICE_GRPC_URL=localhost:50051

# Streaming Configuration
# Follower threshold to go live
MIN_FOLLOWERS_TO_STREAM=100

# RTMP Server
RTMP_SERVER_URL=rtmp://stream.socialink.com/live

# CDN URLs
CDN_HLS_BASE_URL=https://stream.socialink.com/hls
CDN_WEBRTC_BASE_URL=wss://stream.socialink.com/webrtc

# Recording
ENABLE_RECORDING=true
RECORDING_PATH=/var/recordings
